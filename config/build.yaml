include:
  - board: nice_nano_v2
    shield: macropad4x4
    snippet: studio-rpc-usb-uart
  - name: Read matrix
    id: matrix
    run: |
      matrix_content=$(yq eval -o=json -I=0 '.' build.yaml)
      echo "matrix=$matrix_content" >> $GITHUB_OUTPUT
